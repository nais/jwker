apiVersion: jwker.nais.io/v1
kind: Jwker
metadata:
  name: jwker-sample
  namespace: test
spec:
  accessPolicy:
    inbound:
      rules:
        - application: app-a
          namespace: anothernamespace
          cluster: anothercluster
    outbound:
      rules:
        - application: app-b
        - application: foo
          namespace: bar
          cluster: danser
