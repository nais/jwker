apiVersion: jwker.nais.io/v1
kind: Jwker
metadata:
  name: jwker-sample
  namespace: test
spec:
  secretName: jwker-samle-random
  accessPolicy:
    inbound:
      rules:
        - application: app-a
          namespace: anothernamespace
          cluster: anothercluster
    outbound:
      rules:
        - application: foo
          namespace: bar
          cluster: danser
